{
  "actions": {
    "ApplyConcurrent": {
      "document": "This API is used to request concurrency quota.",
      "input": "ApplyConcurrentRequest",
      "name": "Requests concurrency quota",
      "output": "ApplyConcurrentResponse",
      "status": "online"
    },
    "CreateSession": {
      "document": "This API is used to create a session.",
      "input": "CreateSessionRequest",
      "name": "Creates a session",
      "output": "CreateSessionResponse",
      "status": "online"
    },
    "DestroySession": {
      "document": "This API is used to terminate a session.",
      "input": "DestroySessionRequest",
      "name": "Terminates a session",
      "output": "DestroySessionResponse",
      "status": "online"
    }
  },
  "metadata": {
    "apiVersion": "2022-01-10",
    "serviceNameCN": "应用云渲染",
    "serviceShortName": "car"
  },
  "objects": {
    "ApplyConcurrentRequest": {
      "document": "ApplyConcurrent request structure.",
      "members": [
        {
          "default": "",
          "disabled": false,
          "document": "The user’s unique ID. Tencent Cloud does not parse the ID. You need to manage your own user IDs. Based on your needs, you can either define unique IDs for users or use timestamps to generate random IDs. Make sure the same ID is used when a user reconnects to your application.",
          "example": "cg_user",
          "member": "string",
          "name": "UserId",
          "required": true,
          "type": "string"
        },
        {
          "default": "",
          "disabled": false,
          "document": "Public IP of user’s application client, which is used for nearby scheduling.",
          "example": "125.127.178.228",
          "member": "string",
          "name": "UserIp",
          "required": true,
          "type": "string"
        },
        {
          "default": "",
          "disabled": false,
          "document": "The project ID.",
          "example": "cap-abcdefgh",
          "member": "string",
          "name": "ProjectId",
          "required": true,
          "type": "string"
        },
        {
          "default": "",
          "disabled": false,
          "document": "The application version ID.",
          "example": "ver-1a2b3c4d",
          "member": "string",
          "name": "ApplicationVersionId",
          "required": false,
          "type": "string"
        },
        {
          "default": "",
          "disabled": false,
          "document": "Application ID, which is used only by the multi-application project to specify applications. For a single-application project, this parameter is ignored, and the application bound to the project will be used.",
          "example": "app-a1b2c3d4",
          "member": "string",
          "name": "ApplicationId",
          "required": false,
          "type": "string"
        }
      ],
      "type": "object"
    },
    "ApplyConcurrentResponse": {
      "document": "ApplyConcurrent response structure.",
      "members": [
        {
          "document": "The unique request ID, which is returned for each request. RequestId is required for locating a problem.",
          "member": "string",
          "name": "RequestId",
          "type": "string"
        }
      ],
      "type": "object"
    },
    "CreateSessionRequest": {
      "document": "CreateSession request structure.",
      "members": [
        {
          "disabled": false,
          "document": "The user’s unique ID. Tencent Cloud does not parse the ID. You need to manage your own user IDs. Based on your needs, you can either define unique IDs for users or use timestamps to generate random IDs. Make sure the same ID is used when a user reconnects to your application.",
          "example": "cg_user",
          "member": "string",
          "name": "UserId",
          "required": true,
          "type": "string"
        },
        {
          "disabled": false,
          "document": "Public IP of user’s application client, which is used for nearby scheduling.",
          "example": "125.127.178.228",
          "member": "string",
          "name": "UserIp",
          "required": true,
          "type": "string"
        },
        {
          "disabled": false,
          "document": "The client-side session data, which is obtained from the SDK. If `RunMode` is `RunWithoutClient`, this parameter can be null.",
          "example": "eyJhYmMiOjEyM30=",
          "member": "string",
          "name": "ClientSession",
          "required": false,
          "type": "string"
        },
        {
          "disabled": false,
          "document": "The on-cloud running mode.\n`RunWithoutClient`: Keep the application running on the cloud even when there are no client connections.\nEmpty string (default): Keep the application running on the cloud only when there are client connections.",
          "example": "RunWithoutClient",
          "member": "string",
          "name": "RunMode",
          "required": false,
          "type": "string"
        },
        {
          "disabled": false,
          "document": "Application startup parameter.\nIf the user requests a multi-application project or a prelaunch-disabled single-application project, this parameter takes effect.\nIf the user requests a prelaunch-enabled single-application project, this parameter is invalid.",
          "example": "StartFor=1",
          "member": "string",
          "name": "ApplicationParameters",
          "required": false,
          "type": "string"
        },
        {
          "disabled": false,
          "document": "The user ID of the host in **multi-person interaction** scenarios, which is required.\nIf the current user is the host, `HostUserId` must be the same as their `UserId`; otherwise, `HostUserId` should be the host's `UserId`.",
          "example": "host_user_id",
          "member": "string",
          "name": "HostUserId",
          "required": false,
          "type": "string"
        },
        {
          "disabled": false,
          "document": "The role in **multi-person interaction** scenarios. Valid values:\n`Player`: A user who can operate an application by using a keyboard and mouse\n`Viewer`: A user who can only watch the video in the room but cannot operate the application",
          "example": "Player",
          "member": "string",
          "name": "Role",
          "required": false,
          "type": "string"
        }
      ],
      "type": "object"
    },
    "CreateSessionResponse": {
      "document": "CreateSession response structure.",
      "members": [
        {
          "disabled": false,
          "document": "The server-side session data, which is returned to the SDK.",
          "example": "eyJhYmMiOjEyM30=",
          "member": "string",
          "name": "ServerSession",
          "required": true,
          "type": "string",
          "value_allowed_null": false
        },
        {
          "document": "The unique request ID, which is returned for each request. RequestId is required for locating a problem.",
          "member": "string",
          "name": "RequestId",
          "type": "string"
        }
      ],
      "type": "object"
    },
    "DestroySessionRequest": {
      "document": "DestroySession request structure.",
      "members": [
        {
          "default": "",
          "disabled": false,
          "document": "The user’s unique ID. Tencent Cloud does not parse the ID. You need to manage your own user IDs. Based on your needs, you can either define unique IDs for users or use timestamps to generate random IDs. Make sure the same ID is used when a user reconnects to your application.",
          "example": "cg_user",
          "member": "string",
          "name": "UserId",
          "required": true,
          "type": "string"
        }
      ],
      "type": "object"
    },
    "DestroySessionResponse": {
      "document": "DestroySession response structure.",
      "members": [
        {
          "document": "The unique request ID, which is returned for each request. RequestId is required for locating a problem.",
          "member": "string",
          "name": "RequestId",
          "type": "string"
        }
      ],
      "type": "object"
    }
  },
  "version": "1.0"
}